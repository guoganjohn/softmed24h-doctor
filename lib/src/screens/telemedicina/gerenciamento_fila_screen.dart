import 'package:flutter/material.dart';

class GerenciamentoFilaScreen extends StatelessWidget {
  const GerenciamentoFilaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Conteúdo de Gerenciamento de Fila'),
    );
  }
}
