import 'package:flutter/material.dart';

class TreinamentosScreen extends StatelessWidget {
  const TreinamentosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Conteúdo de Treinamentos'),
    );
  }
}
