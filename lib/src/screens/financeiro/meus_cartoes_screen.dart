import 'package:flutter/material.dart';

class MeusCartoesScreen extends StatelessWidget {
  const MeusCartoesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Conteúdo de Meus Cartões'),
    );
  }
}
