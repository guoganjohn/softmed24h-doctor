import 'package:flutter/material.dart';

class MeusSaquesScreen extends StatelessWidget {
  const MeusSaquesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Conteúdo de Meus Saques'),
    );
  }
}
